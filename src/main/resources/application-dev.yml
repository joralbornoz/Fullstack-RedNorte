server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_rednorte_reasignacion
    username: root
    password: "password"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true