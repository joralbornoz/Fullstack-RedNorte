spring:
  datasource:
    url: jdbc:mysql://localhost:3307/rednorte_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: user_rednorte
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect