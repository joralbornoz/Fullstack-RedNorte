server:
  port: 8001

spring:
  datasource:
    url: jdbc:mysql://db-usuarios:3306/rednorte_usuarios?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: user_usuarios
    password: devpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    root: INFO
    com.rednorte.msusuarios: INFO