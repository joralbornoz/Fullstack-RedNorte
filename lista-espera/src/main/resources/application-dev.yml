spring:
  datasource:
    url: jdbc:mysql://localhost:3307/db_lista_espera?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: user_lista_espera
    password: devpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect